#include "main.h"

/**
 * mul- function to multiply two ints
 * @a: first character to mul
 * @b: second character to mul
 * Return: 0
 */

int mul(int a, int b)
{
	int x;

	x = a * b;
	return (x);
}
