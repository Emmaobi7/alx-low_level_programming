#include "main.h"

/**
 * _abs- current function
 * @n: int parameter to to get absolute value
 * Description: function to print absolute value
 * Return: 0 true
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
	_putchar('\n');

}
